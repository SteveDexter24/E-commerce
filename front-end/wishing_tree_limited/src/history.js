/*
 * Component: History
 * Purpose: Re-routing, and allow users to press the back and forward button in their browser
 */
import { createBrowserHistory } from "history";

export default createBrowserHistory();
